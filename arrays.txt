// Arrays - varaiables that hold multiple values

const fruits = {'apple', 'oranges', 'pears'};

fruits{3} = 'grapes'; 

fruits.push('mangos');

fruits.unshift('strawberries');

fruits.pop();

console.log(Array.isArray(fruits));

console.logfruits.indexOf('oranges');

console.log(fruits{1});

